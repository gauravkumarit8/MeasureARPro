<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Exports (PDF, QR bitmap, watermarked images) live in the app's private
         cache dir and are shared out via FileProvider content:// URIs only. -->
    <cache-path name="exports" path="exports/" />
</paths>
